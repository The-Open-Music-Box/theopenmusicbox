# Dependencies for macOS
-r requirements_base.txt

# Audio - Specific version for macOS
# Note: Using simpleaudio instead of pygame for better compatibility
# Install first: brew install portaudio
simpleaudio==1.0.4

# Optional dependencies for development
pytest==8.0.2
pytest-cov==4.1.0

# Recommended pip update:
# pip install --upgrade pip