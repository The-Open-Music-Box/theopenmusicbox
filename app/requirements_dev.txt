# Development specific dependencies (for macOS or general non-Pi development)
-r requirements_base.txt

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-asyncio>=0.18.0

# Pygame and its dependencies (if needed for local dev/testing of audio features)
# These might be optional if using mocks extensively for Pi hardware.
pygame==2.6.1
numpy>=1.26.0

# Optional: Linters and formatters (uncomment if used)
# flake8
# black
# mypy

# Optional: Mocking libraries for Pi-specific hardware if not using the actual ones
# For example, if RPi.GPIO is not available on the dev machine:
# mock-rpi-gpio
