# TheMusicBox Backend Configuration (.env)
# This file overrides the default values set in config.py.
# Uncomment and set any value you wish to override.

# DEBUG mode (default: True)
# DEBUG=True

# Enable auto-reload for development (default: True)
# USE_RELOADER=True

# Host for Socket.IO server (default: 0.0.0.0)
# SOCKETIO_HOST=0.0.0.0

# Port for Socket.IO server (default: 5004)
# SOCKETIO_PORT=5004

# Directory for uploaded files (default: uploads)
# UPLOAD_FOLDER=uploads

# Path to SQLite database file (default: database/app.db)
# DB_FILE=database/app.db

# CORS allowed origins, separated by semicolons
# CORS_ALLOWED_ORIGINS=http://localhost:8081;http://127.0.0.1:8081

# Logging level (default: INFO)
# LOG_LEVEL=INFO

# Logging format (default: %(asctime)s - %(name)s - %(levelname)s - %(message)s)
# LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s

# Path to log file (default: logs/app.log)
# LOG_FILE=logs/app.log

# Use mock hardware (default: False)
# USE_MOCK_HARDWARE=False

# ASGI app module for Uvicorn (default: app.main:app_sio)
# APP_MODULE=app.main:app_sio

# Enable Uvicorn reload (default: True)
# UVICORN_RELOAD=True