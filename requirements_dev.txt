# Development specific dependencies for TheOpenMusicBox (macOS, Linux, CI, etc.)
# Includes linting, formatting, security, and test tools

# Linting and formatting
black
flake8
isort

# Security
bandit

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-mock
pytest-asyncio>=0.18.0

# Pygame and its dependencies (for local dev/testing of audio features)
# These might be optional if using mocks extensively for Pi hardware.
pygame==2.6.1
numpy>=1.26.0

# Documentation
sphinx
sphinx-rtd-theme

# Pre-commit hooks
pre-commit

# Other dev tools
# Add here as needed
